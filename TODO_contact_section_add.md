# TODO: Industrial contact section on landing page

- [ ] Insert a new `<section id="contact">` on `index.html` (landing page) between the How It Works section and the Footer.
- [ ] Add an industrial design layout: map card, contact form, and direct contact tiles.
- [ ] Add minimal CSS in `index.css` for the new section (if required).
- [ ] Ensure the sticky header doesn’t hide the section top (add padding/anchor offset if needed).

