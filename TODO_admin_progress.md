# TODO Admin Progress Tracker

- [ ] Implement Event Edit modal + edit buttons in `admin.html`
- [ ] Implement edit/reactivate logic in `admin.js` (loadEventById/openEditEvent/updateEvent/reactivateEvent)
- [ ] Update Events table to show Edit + Reactivate actions
- [ ] Add backend endpoint `GET /api/admin/events` (admin-only) to return active + inactive events (required for reactivate)
- [ ] Implement minimal styles in `admin.css` for the edit modal/buttons (if missing)
- [ ] Verify: run backend, open `/admin.html`, login as admin, test add/edit/deactivate/reactivate

