# TODO — Admin dashboard theme toggle

- [ ] Add theme toggle button + icon to `admin.html`
- [ ] Implement `applyTheme()` and `toggleTheme()` in `admin.html` (reuse logic from `index.html`)
- [ ] Ensure admin dashboard uses `data-theme` variables from `index.css`

Done in code when merged.
